import Avatar from "./avatar";
import Text from "./text";

let Card = (props) => {
    return (
        <article id="card">
            <Avatar />
            <Text />
            <Text />
        </article>
    );
}

export default Card;